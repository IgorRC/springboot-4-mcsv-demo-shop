version: '3.9'

services:
  mysql:
    image: mysql:8.0
    container_name: mysql_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: sasa1234
      MYSQL_DATABASE: db_springboot_cloud
      MYSQL_USER: igor
      MYSQL_PASSWORD: capibara
    ports:
      - "3306:3306"
    volumes:
      - mscv_products_mysql_data:/var/lib/mysql

volumes:
  mscv_products_mysql_data: